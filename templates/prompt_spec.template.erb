require 'spec_helper'
require 'story_helper'

find_stories('<%= @name.downcase %>').each do |story|
  describe story do
    # Put the expectations for your new story here.
    # Here, you are given the text of the story in the `story` variable,
    # to test whatever you'd like. Any valid rspec tests will work here.

    # Refer to the example (spec/prompts/dr_seus_special_spec.rb) for syntax,
    # and the matcher library (spec/story_helper.rb) for available constraints.
    # or, feel free to add your own!
    
    # Happy coding!
  end
end
